package live.senya.garrely.entity

import java.util.concurrent.TimeUnit

//todo move to buildconfig
object Constants {
    
    val CACHE_FADE_TIME = TimeUnit.HOURS.toMillis(24)
    const val USE_SAFE_SEARCH = true
}